#ifndef ARITHMETICS_CUH
#define ARITHMETICS_CUH

#include "cuda_runtime.h"

//__global__ float* k_add(float* v, int nv, float x, float* res);
//__global__ float* k_add(float* v1, float* v2, int nv, float* res);
//
//__global__ float* k_add(float* v, int nv, float x, float* res);
//__global__ float* k_add(float* v1, float* v2, int nv, float* res);
//
//__global__ float* k_add(float* v, int nv, float x, float* res);
//__global__ float* k_add(float* v1, float* v2, int nv, float* res);
//
//__global__ float* k_add(float* v, int nv, float x, float* res);
//__global__ float* k_add(float* v1, float* v2, int nv, float* res);
//
//__global__ float* k_add(float* v, int nv, float x, float* res);
//__global__ float* k_add(float* v1, float* v2, int nv, float* res);

#endif